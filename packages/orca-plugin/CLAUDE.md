<!-- MESH:START — generated by fc/scripts/gen-mesh.mjs from fc/mesh.json. Do not edit by hand. -->

## Cross-repo mesh — `orca-plugin`

**Orca integration**, one package of [`createos-plugin`](https://github.com/NodeOps-app/createos-plugin). Pointer only — Orca cannot install from a monorepo subdirectory, so the implementation lives in its own repo.

### What ripples into this package

No implementation here — the code lives in its own repository. Changes land there, not in this directory.

User-visible changes here are documented at `Integrations/Orca` in [`createos-v2-landing`](https://github.com/NodeOps-app/createos-v2-landing). Keep the two in step.

The full repo graph, what counts as a shared surface, and the ripple
protocol live in the monorepo root [`CLAUDE.md`](../../CLAUDE.md), which is
loaded automatically from this directory.

<!-- MESH:END -->

{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "createos-sandbox",
  "displayName": "CreateOS Sandbox",
  "version": "0.1.0",
  "description": "Run ad-hoc, heavy, or untrusted code OFF your machine in disposable CreateOS Firecracker microVMs. One-shot offload (stage files → exec → pull artifacts → auto-destroy) plus an opt-in reusable box with two-way file sync for live dev loops.",
  "author": { "name": "NodeOps", "url": "https://createos.nodeops.network" },
  "homepage": "https://createos.nodeops.network",
  "keywords": ["sandbox", "createos", "firecracker", "remote-exec", "isolation", "microvm"],
  "hooks": "./hooks/hooks.json"
}
